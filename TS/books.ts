// Using Book tuples
let Books: [Number, String, String, String, boolean ] = [1, "The great controversy", "Ellen G.white", "BSN", true]
    let count = 1;
    Books.forEach(n => {
        Books[0] = count;
        console.log(Books)
        count++
    })

    // function to return a collection of all books
    function Books(Inter: arr): Array {
        
    }